version: '2'
services:
    thebuyback-mongodb:
        image: mongo:3.2.10
        ports:
            - "27017:27017"
        command: mongos --configdb thebuyback-mongodb-config
    thebuyback-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    thebuyback-mongodb-config:
        image: mongo:3.2.10
        container_name: thebuyback-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
